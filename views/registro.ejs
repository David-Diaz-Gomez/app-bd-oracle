<!-- archivo: views/registro.ejs -->
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Registro de Usuario</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700,900&display=swap" rel="stylesheet">
  <style>
    body {
      padding: 100px 0;
      background: linear-gradient(to right, #00bcd4, #8bc34a);
      font-family: 'Roboto', sans-serif;
      font-size: 18px;
      line-height: 1.5;
      color: #222;
    }
    .header{
        color: white;
        margin-bottom: 3em;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
    }
    .container {
      max-width: 1230px;
      width: 100%;
    }
    .form-wrap {
      background: #fff;
      max-width: 850px;
      padding: 50px;
      margin: 0 auto;
      border-radius: 10px;
      box-shadow: 0 0 40px rgba(0, 0, 0, 0.15);
      position: relative;
    }
    .form-wrap:before {
      content: "";
      width: 90%;
      height: calc(100% + 60px);
      position: absolute;
      top: -30px;
      left: 0;
      right: 0;
      margin: auto;
      background: #00bcd9;
      opacity: 0.8;
      border-radius: 10px;
      z-index: -1;
    }
    .form-group label {
      font-size: 18px;
      color: #000;
    }
    .form-control {
      height: 50px;
      background: #ecf0f4;
      border: none;
      padding: 0 15px;
      font-size: 16px;
      transition: all 0.3s ease-in-out;
    }
    .form-control:focus {
      border-color: #00bcd9;
      box-shadow: 0 0 20px rgba(0, 0, 0, .1);
    }
    textarea.form-control {
      height: 160px;
      resize: none;
    }
    .btn {
      padding: .657rem .75rem;
      font-size: 18px;
    }
    .btn-primary {
      background-color: #00bcd9;
      border-color: #00bcd9;
    }
    .btn-primary:hover {
      background-color: #ffffff;
      color: #00bcd9;
      border-color: #00bcd9;
      box-shadow: 0 0 20px rgba(0, 0, 0, .1);
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="header text-center">
      <h1>Registro de Usuario</h1>
      <p>Completa el formulario para registrarte</p>
    </header>
    <div class="form-wrap">
      <form action="/enviar-validacion" method="POST" class="needs-validation" novalidate>
  <div class="row">
    <div class="col-md-6 form-group">
      <label for="consecUser">ID Usuario</label>
      <input type="text" class="form-control" name="consecUser" required pattern="[A-Za-z0-9]+">
      <div class="valid-feedback">Correcto</div>
      <div class="invalid-feedback">ID alfanumérico obligatorio (ej: A123).</div>
    </div>
    <div class="col-md-6 form-group">
      <label for="nombre">Nombre</label>
      <input type="text" class="form-control" name="nombre" required pattern="[A-Za-z\s]+">
      <div class="valid-feedback">¡Bien!</div>
      <div class="invalid-feedback">Solo letras y espacios.</div>
    </div>
  </div>

  <div class="row mt-3">
    <div class="col-md-6 form-group">
      <label for="apellido">Apellido</label>
      <input type="text" class="form-control" name="apellido" required pattern="[A-Za-z\s]+">
      <div class="valid-feedback">Perfecto</div>
      <div class="invalid-feedback">Solo letras y espacios.</div>
    </div>
    <div class="col-md-6 form-group">
      <label for="user">Usuario</label>
      <input type="text" class="form-control" name="user" required>
      <div class="valid-feedback">Correcto</div>
      <div class="invalid-feedback">Campo obligatorio.</div>
    </div>
  </div>

  <div class="row mt-3">
    <div class="col-md-6 form-group">
      <label for="fechaRegistro">Fecha de Registro</label>
      <input type="date" class="form-control" name="fechaRegistro" required>
      <div class="valid-feedback">Fecha válida</div>
      <div class="invalid-feedback">Este campo es obligatorio.</div>
    </div>
    <div class="col-md-6 form-group">
      <label for="email">Correo electrónico</label>
      <input type="email" class="form-control" name="email" required>
      <div class="valid-feedback">Correo válido</div>
      <div class="invalid-feedback">Ingresa un correo electrónico válido.</div>
    </div>
  </div>

  <div class="row mt-3">
    <div class="col-md-6 form-group">
      <label for="celular">Celular</label>
      <input type="text" class="form-control" name="celular" required pattern="[0-9]+">
      <div class="valid-feedback">Número válido</div>
      <div class="invalid-feedback">Ingresa solo números.</div>
    </div>
    <div class="col-md-6 form-group">
      <label for="codUbica">País</label>
      <select class="form-control" name="codUbica" required>
        <option value="">Seleccione un país</option>
        <% ubicaciones.forEach(([codigo, nombre]) => { %>
          <option value="<%= codigo %>"><%= nombre %></option>
        <% }); %>
      </select>
      <div class="valid-feedback">País seleccionado</div>
      <div class="invalid-feedback">Por favor, elige un país.</div>
    </div>
  </div>

  <div class="row mt-4">
    <div class="col-12 text-center">
      <button type="submit" class="btn btn-primary">Enviar correo de validación</button>
    </div>
  </div>
</form>

    </div>
  </div>
  <script>
  (function () {
    'use strict';
    var forms = document.querySelectorAll('.needs-validation');
    Array.prototype.slice.call(forms).forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  })();
</script>
</body>
</html>